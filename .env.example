# Create a local .env file in the app you run. Never commit the real value.
TYPESAFE_API_KEY=replace-with-your-own-key

# Pin the evaluated model so thresholds do not silently change.
TYPESAFE_MODEL=jev-1.13.0
